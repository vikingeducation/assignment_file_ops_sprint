require 'dictionary_ui'
require 'dictionary'
require 'dictionary_loader'
require 'dictionary_searcher'
require 'results_saver'

describe DictionaryUI do
  describe '#run' do

    # subject { DictionaryUI.new }
    # let(:stats)       { }
    # let(:dictionary)  { instance_double("Dictionary", stats: {word_count: 7}, word_bank: ["one"]) }
    # let(:dict_loader) { instance_double("DictionaryLoader", load: dictionary) }
    # before do
    #   allow(subject).to receive(:gets).and_return(' ')
    #   allow(DictionaryLoader).to receive(:new).and_return(dict_loader)
      
    # end

    # it 'prints directions to the screen' do
    #   expect(subject).to receive(:puts).at_least(1).times
    #   subject.run
    # end

    # it 'makes a call to dictionary loader instance with file path' do
    #   expect(dict_loader).to receive(:load).with(' ')
    # end
  end
end
